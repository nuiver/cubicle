<h1> Deals of <%= current_user.profile.first_name %> </h1>

<% @deals.each do |deal| %>
  <table>
    <tr>
      <td><%= deal.id %></td>
      <td><%= deal.piece.name %></td>
    </tr>
  </table>
<% end %>
