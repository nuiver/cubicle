<nav class="navbar">
  <div class="container">
    <div class="dropdown_box hidden">
      <%= render "layouts/menu_product_types" %>
    </div>
    <ul class="nav">
      <li><%= link_to 'FIND BY STYLE', "#", id: 'nav_product', remote: true %></li>
      <% if user_signed_in? %>
        <% if current_user.has_hearted? %>
          <li><%= link_to "HEARTED PIECES", pieces_hearted_path, method: :get %></li>
        <% end %>
        <% if current_user.has_pieces? %>
          <li><%= link_to "MY PIECES", pieces_owned_path, method: :get %></li>
        <% end %>
        <% unless cannot?(:create, Piece) %>
          <li><%= link_to "ADD NEW PIECE", new_piece_path, id: 'add_piece' %></li>
        <% end %>
        <li><%= link_to "MY DEALS", deals_path %></li>
        <li><%= link_to "HIRED OUT", owned_deals_path %></li>

      <% end %>

      <% if user_signed_in? && current_user.has_open_proposals? %>
        <li class="rightnav">
          <%= link_to "", owned_deals_path, class: "fa fa-bell" %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>
